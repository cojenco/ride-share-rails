<%= @trip.id %>
   <br>
<%= @trip.date %>
   <br>
<%= @trip.passenger_id %>
   <br>
<%= @trip.driver_id %>

<%= link_to "Delete", trip_path(@trip.id), method: :delete, data: {confirm: "Are you sure you want to delete trip Nr: #{@trip.id}?"} %>
<%= link_to "Edit", edit_trip_path(@trip.id)%>